<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Parliamentary Member</title>
</head>
<body>
<h1>Edit Parliamentary Member</h1>
<form action="/member/<%= member._id %>/update" method="POST">
    <label>Name: <input type="text" name="name" value="<%= member.name %>" required></label><br>
    <label>Party: <input type="text" name="party" value="<%= member.party %>" required></label><br>
    <label>Position:
        <select name="position" required>
            <option value="Chairman" <%= member.position === 'Chairman' ? 'selected' : '' %>>Chairman</option>
            <option value="Minister" <%= member.position === 'Minister' ? 'selected' : '' %>>Minister</option>
        </select>
    </label><br>
    <label>Employment Date: <input type="date" name="employmentDate" value="<%= member.employmentDate.toISOString().slice(0, 10) %>" required></label><br>
    <button type="submit">Update Member</button>
</form>
<a href="/">Back to list</a>
</body>
</html>